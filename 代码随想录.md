# 代码随想录

## 编程语言基础

## 数组

### 1. 数组理论基础

数组是存放在连续内存空间上的相同类型数据的集合

数组的元素是不能删的，只能覆盖

### 2. 二分查找

循环不变量规则，每一次边界的处理都要根据区间的定义来操作

### 3. 移除元素

双指针法（快慢指针法）：通过一个快指针和慢指针在一个for循环下完成两个for循环的工作

双指针法（快慢指针法）在数组和链表的操作中是非常常见的，很多考察数组、链表、字符串等操作的面试题，都使用双指针法

### 4. 有序数组的平方

> 由后向前遍历，LeetCode 844 比较退格的字符串与此类似

### 5. 长度最小的子数组

滑动窗口：不断调节子序列的起始位置和终止位置，从而得出我们想要的结果

主要确定如下三点：

- 窗口内是什么
- 如何移动窗口的起始位置
- 如何移动窗口的结束位置

> 此题也可以使用前缀和 + 二分查找的方式，时间复杂度为 $O(n\log{n})$。前缀和记录从头累加到当前元素的总和。对于每一个前缀和，在数组中二分查找比其大目标值的前缀和
>
> - 水果成篮：滑动窗口+哈希表的思路没有问题，哈希表寻找一种水果最左端位置思路也没有问题，实现时可以采取投票思路，右端向右遍历对应项累加，左端向右遍历对应项累减
> - 最小覆盖子串：判断可行解条件为待遍历字符串哈希表包含匹配字符串哈希表中所有字符，且对应个数都不小于匹配字符串哈希表各个字符的个数。可以在类内创建辅助函数简化操作逻辑。`substr()`方法会复制子串，最好是更新起止索引。由于字符串由`char`组成，可以创建一个128维的数组代替哈希表

### 6. 螺旋矩阵 II

坚持循环不变量原则

注意二维数组初始化方式，可以是`vector result(n, vector<int>(n))`

> 螺旋矩阵一题，每次更新边界条件时可以进行越界判断，这样可以不用进行元素标记或向量长度校验

### 7. 区间和

前缀和：重复利用计算过的子数组之和，从而降低区间查询需要累加计算的次数

### 8. 开发商购买土地

行和列分别执行前缀和操作，注意ACM模式下数组初始化

### 9. 总结篇

数组可以方便的通过下标索引的方式获取到下标对应的数据

vector的底层实现是array，严格来讲vector是容器，不是数组

- 二分法：循环不变量原则
- 双指针法：通过一个快指针和慢指针在一个for循环下完成两个for循环的工作
- 滑动窗口：根据当前子序列和大小的情况，不断调节子序列的起始位置
- 模拟行为
- 前缀和

## 链表

### 1. 链表理论基础

链表是一种通过指针串联在一起的线性结构，每一个节点由两部分组成，一个是数据域一个是指针域，最后一个节点的指针域指向null

链表的入口节点称为链表的头结点也就是head

链表的类型：

- 单链表
- 双链表：每一个节点有两个指针域，一个指向下一个节点，一个指向上一个节点
- 循环链表：链表首尾相连，可以用来解决约瑟夫环问题

链表在内存中不是连续分布的，通过指针域的指针链接在内存中的各个节点

链表节点定义方式，如果使用默认构造函数，在初始化的时候不能直接给变量赋值

```C++
struct ListNode {
    int val; // 节点上存储的元素
    ListNode *next; // 指向下一个节点的指针
    ListNode(int x) : val(x), next(NULL) {} // 节点的构造函数
}

ListNode* head = new ListNode(5); // 初始化节点
```

### 2. 移除链表元素

链表操作的两种方式：

- 直接使用原来的链表来进行删除操作
- 设置一个虚拟头结点再进行删除操作

此题目也可以递归完成

### 3. 设计链表

### 4. 反转链表

递归法可以理解为把迭代法循环的部分抽象为一个函数
